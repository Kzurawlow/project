#pragma once
/**
* file with function that generates random numbers
* Used when generating numbers from dead enemy and displaying enemies randomly
*/
#include<iostream>
using namespace std;
//! A generateRandom() function that generates random numbers.
	/*!
	  \param max an integer argument max.
	*/
float generateRandom(int max);
